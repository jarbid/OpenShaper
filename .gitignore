node_modules/
dist/
.turbo/
.wrangler/
*.tsbuildinfo
.env
.env.local
coverage/
.vite-react-ssg-temp/
# Tauri
apps/desktop/src-tauri/target/
# Gradle (golden-exporter recompiles legacy sources)
tools/**/build/
tools/**/.gradle/
.gradle/
# OS
.DS_Store
Thumbs.db
nul
# Test / browser-automation artifacts
.playwright-mcp/
test-results/
playwright-report/
# Local dev screenshots dropped in the repo root (scratch captures)
/*.png
# Local scratch board files used to test import (real third-party designs - never commit)
/Sample board imports/
# Local Claude Code config (machine-specific; keep CLAUDE.md and agents/ tracked)
.claude/settings.json
.claude/settings.local.json
.claude/hooks/
.claude/skills/
.mcp.json
# Agent-workflow scratch output (task reports/diffs — local process artifacts)
.superpowers/
# Locally collected sample board files (third-party designs; never publish)
Sample board imports/
