# Intentional divergences from BoardCAD-LE

The ledger required by CLAUDE.md principle 2 (golden-data rule, ported phase).
Every place where OpenShaper _deliberately_ differs from legacy behavior gets an
entry here: what changed, why, the magnitude, and the replacement oracle that
proves the new behavior is right. Accidental drift is still a bug — if a golden
test fails and there's no entry here, the code is wrong, not the fixture.

Adding an entry requires:

1. **A better oracle** — analytic cases, convergence tests, or a published
   reference that the new behavior is verified against (legacy stops being the
   ground truth for that value).
2. **Regenerated or re-banded fixtures** — the golden test changes in the same
   commit, with the new tolerance derivation documented.
3. **A row below.**

| Date       | Subsystem                                               | What differs                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | Magnitude vs legacy                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | Why                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | Oracle                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | Commit                             |
| ---------- | ------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------- |
| 2026-06-11 | `getVolume` / `getCenterOfMass` (kernel)                | Longitudinal integral defaults to `adaptiveSimpson` (relTol `1e-5`) instead of the legacy fixed `VOLUME_Y_SPLITS=30` / `MASS_Y_SPLITS=10` Simpson. Inner cross-section trapezoid unchanged.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | Volume: ≤ 0.016% vs legacy fixed-split (control-point boards), ≤ 1.3e-4 rel (sLinear longboard). CoM: ≤ 0.072 cm. All well inside the 1% / 0.5 cm golden bands. (The `1e-5` relTol was chosen because `1e-4` still leaves the funboard ~0.18% short of converged; `1e-5` lands within ~1e-4 of the 1e-9 reference.)                                                                                                                                                                                                                                                                                                 | Legacy hard-coded 30/10 panels (CLAUDE.md principle 3). Adaptive refines only where the area-vs-x curve needs it, so it is at least as accurate everywhere for ~13–260 integrand calls/board.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | Convergence: adaptive default agrees with a 4×-finer fixed Simpson to < 0.5% (`board.integration.test.ts`) and to < 1e-3 over the sLinear area curve (`board.slinear.test.ts`). Legacy splits still reproducible via `IntegrationOptions`.                                                                                                                                                                                                                                                                                                                                                                                                                                                         | feat/adaptive-integration-default  |
| 2026-06-11 | `getArea` (kernel)                                      | Planshape-area integral defaults to `adaptiveSimpson` (relTol `1e-5`) instead of the legacy fixed `AREA_SPLITS=10` Simpson.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | ≤ 0.75% larger than legacy (longboard; shortboard 0.42%, funboard 0.52%) — AREA_SPLITS was the least-converged legacy resolution.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | Same as above; `AREA_SPLITS=10` left the longboard's width integral ~0.63–0.75% short of converged.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | Adaptive default equals a 1e-9 adaptive integral of the width to < 0.01% (`board.integration.test.ts`); still inside the 1% golden area band. `getArea(b, 10)` reproduces the legacy value bit-for-bit.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | feat/adaptive-integration-default  |
| 2026-06-14 | `fins` (kernel)                                         | Replaces the legacy `BezierBoard.mFins` flat `double[9]` + free-text `mFinType` with a parametric, system-aware `FinConfig` (named setup + fin system + per-fin specs) resolved to geometry on demand by `resolveFins`. Placement is parametric to the board (inset-from-rail follows the outline; base rides the bottom rocker), and FCS II / FCS x2 / Futures / glass-on box footprints are modelled (legacy modelled none). Blade silhouettes (single / noserider / keel / thruster) are traced from FinFoil reference outlines (`docs/specs/fins/*.foil` → `fin-templates.generated.ts`); legacy drew fins as three plain lines.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | Total — the legacy default was all-zeros (no usable placement) and stored absolute coords that did not track shape edits. No legacy numeric value is reproduced.                                                                                                                                                                                                                                                                                                                                                                                                                                                    | Legacy fin data was unusable (zeros), absolute (broke on scale/edit), and modelled no fin systems — the rebuild requires editable, system-accurate, shape-coupled fins in 2D/3D and every export.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | Analytic: placement, inset/surface coupling, toe geometry, tail-end detection pinned in `fins.test.ts`; `SYSTEM_BOX` pinned to manufacturer hardware dimensions. No legacy oracle (model superseded, not approximated).                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | (this branch)                      |
| 2026-06-19 | `enforceJunctions` outline tip pin (store)              | The outline centreline pin snaps the **nose** (`knots[last]`, `x = length`) to `y = 0` and leaves the **tail** (`knots[0]`, `x = 0`) free. Legacy JC-1 fully locks **both** outline tips via `setMask(0,0)`. We pin only the nose's `y`; the tail keeps its width. (Also corrects a prior inverted-naming bug that pinned the tail instead of the nose.)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | Tail tip is unconstrained vs legacy's both-tip lock; the nose `y`-pin matches the geometric intent of legacy JC-1's nose endpoint. No legacy numeric value is reproduced (mask machinery not modeled).                                                                                                                                                                                                                                                                                                                                                                                                              | The nose is the pointed tip → half-width 0 on the stringer; the tail carries legitimate width on wide / fish / square tails, so a full both-tip `y=0` lock would be geometrically wrong for the tail. Legacy's mask-based full lock is a stricter UX guard deferred to the Stage B mask layer.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | Analytic / geometric: pinned in `edits.test.ts` ("junction-constraint spec" + the wide-tail regression) — nose snaps to `y=0`, tail width preserved. Oracle is surfboard geometry, not a legacy number.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | fix/junction-outline-tip-pin       |
| 2026-06-19 | `enforceJunctions` endpoint x-masks (store)             | Adds JC-1/JC-2/JC-3 endpoint **x**-masks as post-edit re-snaps: outline tail → `x = 0`; deck & bottom tail → `x = 0`, nose → `x = length` (`lockEndpointsX`, length = outline-derived `getLength`). y preserved. Legacy applies these as live per-knot `setMask(0, …)` drag clamps; the immutable kernel models them as an idempotent re-snap after the edit instead. The legacy full "un-draggable" lock (incl. tail y, and a literally non-grabbable handle) is **not** modeled.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | Behaviourally equivalent for the committed geometry (tips can't end up off-station); differs only in _interaction_ (legacy blocks the drag live; we let it move then re-snap). No legacy numeric value reproduced. Verified no-op on all three bundled `.brd` boards (Δ = 0).                                                                                                                                                                                                                                                                                                                                       | Closes the JC-2/JC-3 gap where a drag could pull a deck/bottom tip off the board ends, and pins the outline tail station. The immutable kernel has no drag-time hook, so a re-snap is the correct model.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | Analytic: pinned in `edits.test.ts` ("JC-1/JC-2/JC-3 endpoint x-masks"); ordering-vs-adjust guarded by `board.adjust.test.ts` ("JC-4-x survives adjust"). Real-board no-op confirmed by an endpoint-span probe.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | fix/junction-outline-tip-pin       |
| 2026-06-19 | `enforceJunctions` tangent-flow locks (store)           | Adds JC-6/JC-7/JC-8 tangent clamps as post-edit re-snaps: outline/deck/bottom kept single-valued in x (`clampMonotonicX`: `toPrev.x ≤ end.x`, `toNext.x ≥ end.x` on segment-driving handles); outline tip inward handles raised to ≥ tip y (`clampHandleFloor`, JC-7); section centre tip inward handles raised to `x ≥ 0` (JC-8). Legacy applies these as live per-handle `handleLocks` clamps during a drag; the immutable kernel models them as an idempotent post-edit clamp. Unused dangling handles are left untouched.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | Behaviourally equivalent for the committed geometry (no folded tangent / inverted tip can be committed); differs only in interaction (live clamp vs post-edit). No legacy numeric value reproduced. Verified no-op on all three bundled `.brd` boards (Δ = 0, incl. tangents).                                                                                                                                                                                                                                                                                                                                      | Closes the last junction gap where a drag could fold a tangent past its endpoint x or invert the planshape at a tip. The immutable kernel has no drag-time hook, so a re-snap is the correct model.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | Analytic: pinned in `edits.test.ts` ("JC-6/JC-7/JC-8 tangent-flow locks"). Real-board no-op (rendered geometry + tangents) confirmed by a before/after `enforceJunctions` probe.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | fix/junction-outline-tip-pin       |
| 2026-06-20 | `adjustCrossectionThickness` gate (store + web)         | JC-4-y modelled as a **commit-pipeline gate**, not a per-knot section y-mask. The `adjustCrossSectionThickness` editor setting (localStorage, default on) drives the store's `adjustThickness` flag; when off, `commit`/`load` skip `adjustCrossSectionsToThicknessAndWidth`, so a rocker/deck/outline edit no longer re-slaves the cross-sections (they keep their own thickness & width). Legacy masks the section centre-endpoint y during a direct section drag; we gate the inverse board→section coupling.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | Same user-facing intent (decouple section thickness from the board). Structurally different: a pipeline gate vs a per-handle drag mask; the direct section-drag mask itself is not modelled. Default-on preserves prior behaviour exactly.                                                                                                                                                                                                                                                                                                                                                                          | The immutable kernel runs the slaving as a whole-board pass in `commit`, so the setting naturally maps to skipping that pass rather than masking a knot.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | Pinned in `board-store.test.ts` ("adjustThickness gate (JC-4-y)") — a global curve edit resizes interior sections when on, leaves them untouched when off; `settings.test.ts` covers the default + v1→v2 migration.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | fix/junction-adjust-thickness-gate |
| 2026-06-25 | concave tail (swallow / fish) — outline cutout (kernel) | The outline may now be **non-monotonic in x** at the tail: a folded notch gives each station an inner wall `y_in > 0` and an outer rail `y_out`. Detected geometrically (`hasTailCutout` = outline min-x tip not at the first sample) and split into `(y_in, y_out)` per station (`outline-cutout.ts`, AIShaper method). `tessellateBoard` lofts such boards as two ±Y half-shells sealed by deck↔bottom notch walls (watertight); `getWidthAtPos` returns the outer rail; planshape area integrates `2·(y_out − y_in)` and cross-section area scales by the lateral compression `(y_out − y_in)/y_out`. Legacy `BezierBoard` modelled the outline as strictly single-valued in x — it could not represent a swallow/fish tail at all. **Limitation:** single notch only (one tip / one `[y_in,y_out]` band per side) — a batwing / multi-point tail (multiple rear points → multiple foam bands) is not supported and renders incorrectly; full support needs a scanline interval model with variable-topology lofting (deferred).                                                                                                                                                                                                                                                                                                                                                                                                                                               | New capability; legacy has no equivalent. **Normal (single-valued) boards are bit-identical** — every existing golden volume/area/mesh value is unchanged (the cutout path is gated on `hasTailCutout`). No legacy numeric value is reproduced or superseded for concave tails.                                                                                                                                                                                                                                                                                                                                     | Legacy hard-assumes a single half-width per station, so swallow / fish / bat tails were impossible. Reading the cutout straight off the outline geometry keeps the "just edit the outline" workflow with no extra parameter, while leaving the legacy single-valued path untouched for every normal board.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | Analytic / geometric: `outline-cutout.test.ts` pins `(y_in,y_out)` against linear fixtures + back-compat (`y_in=0` ⇒ exact `valueAt`); `tessellate.cutout.test.ts` proves the swallow mesh is watertight (zero boundary edges after welding) with a real centerline notch gap; `board.cutout.test.ts` checks planshape-area removed equals an independent ∫ y_in oracle and volume drops below the notch-filled board.                                                                                                                                                                                                                                                                             | feat/concave-tail-outline          |
| 2026-06-24 | `parseS3dx` real-world robustness (io)                  | Four deviations from legacy `S3dxReader`, all to import genuine producer files the legacy mishandles: (1) **Outline fallback** — legacy reads the outline only from `<curveDefTop2>` and NPEs if absent; we fall back to `<curveDefTop1>` (with a warning). (2) **Single-valued-in-x clamp** — each primary curve's knot/handle x is clamped to `[0, length]` and forced non-decreasing; some exports run the deck past the nose while the bottom ends short, so the injected bottom-nose endpoint produced a backward x-step (folded, "flipped" rocker). (3) **Degenerate-section drop** — cross-sections with <3 control points are dropped (with a warning); legacy keeps them. (4) **`<StringerMeasurement>` deck** — when set, the deck curve stores thickness-above-bottom, not absolute z; we reconstruct the absolute deck as `bottom(x) + thickness(x)` by sampling that function within each source segment and re-fitting a cubic with the kernel's `bestFit` (keeping the file's knot stations, reflecting the two outer handles for a smooth tip cap). The earlier per-control-point rocker addition was wrong — a Bézier handle is not a point on the curve, so inflating its y by the rocker height bulged the deck into a double-hump (~0.45–1.3 cm above center thickness). Legacy ignores the flag (renders such boards self-intersecting). Also: a `<Protection>` flag `> 0` throws a clear "password-protected" error instead of legacy's silent empty board. | Files without the offending quirks are byte-identical to legacy (no change). A file missing `curveDefTop2` loads narrower than nominal where legacy crashed; a `StringerMeasurement=1` deck is de-folded/de-spiked, thickness is positive everywhere and no longer bulges above the center thickness; a flat mid-board control-point run is removed. No legacy numeric value is reproduced for these cases (legacy crashed or produced a broken board).                                                                                                                                                             | Legacy crashes or produces unusable geometry on these real exports. A folded curve renders inverted; a thickness-curve deck read as absolute dips below the bottom (negative thickness, spiking rocker), and the naive handle-shift bulges it; a degenerate section pinches the 3D loft AND makes the thickness-slaving pass non-idempotent (it blew a section up to ~380 cm on the next edit). Importing with repair + warnings is strictly better.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | Self-authored synthetic Shape3d exports in `s3dx-reader.test.ts` (built to reproduce each quirk exactly, not redistributed third-party files): the fallback-outline fixture recovers exact length (70″) & thickness (≈2.6″) + fallback warning; the deck-overshoot fixture's curves are asserted non-decreasing in x within `[0,length]` and thickness positive along the whole board; both `StringerMeasurement=1` fixtures' deck thickness never exceeds center thickness by >0.25 cm (no bulge); the degenerate-section fixture (StringerMeasurement=0) deck unchanged + all interior sections ≥3 knots + `adjustCrossSectionsToThicknessAndWidth` idempotent (x1 == x2).                       | (this branch)                      |
| 2026-08-11 | 3D mesh / STL loft (kernel + export)                    | The lofted surface between two cross-sections is now defined by sampling each station's own profile at equal fractional arc length and **blending the two POINTS** (`loft.ts`), instead of blending the two stations' bezier **control points** and evaluating the resulting curve (`interpolateCrossSection`). Knot counts, knot matching and de Casteljau insertion no longer take part in meshing at all; the board-wide knot-normalisation pass (`loftBoard` / `normalizeCrossSectionKnots`) is deleted. `tessellateBoard`, `exportStl` and the 3D guide overlays now share one ring builder. Ring points are also no longer spread uniformly: `ringFractions` builds one per-board ladder that biases them toward curvature (monitor `m = 1 + 0.5·sqrt(|κ|·L)`, taken as the pointwise max across every station so the ladder is identical everywhere), which is the equidistribution form of a CAD tessellator's chord-height tolerance adapted to a fixed point budget. Legacy sampled uniformly in arc length within a normal-angle band. **`interpolateCrossSection` is unchanged and still control-point based** — it remains the definition used by the 2D section preview, `insertCrossSection`, DXF/PDF section curves, HWS ribs, and the volume / centre-of-mass integrand.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | Mesh volume vs reported volume: shortboard −0.042%, funboard −0.389%, longboard −0.289%, and converged (identical at `targetFaceSize 0.5` and at 400×200), so it is a model difference rather than discretisation — all far inside the ±10%/2% mesh band. Golden fixtures do **not** move: `interp.golden.json` `morph`, `golden.json` volume/area/CoM, and the sLinear areas are all untouched, because none of those paths changed. Constant-shape lofts become exact rather than approximate: STL band jump 0.07 cm → 3.2e-6 cm, ring-to-ring jump 0.05 → 1.5e-6 cm, sweep second difference 1.6e-3 → 1.1e-6 cm. Ring ladder: worst chord deviation on an `80-20-hard` rail at default viewport quality 0.253 cm → 0.065 cm, at the cost of the longest ring gap growing 2.05 cm → 3.37 cm on the flat bottom (whose own chord error stays under 0.003 cm). | Control-point blending pairs knots **by array index**, so a station's knot is blended with whichever knot shares its list position — which can sit anywhere on the other profile — and `lerpKnot` lerps the tangent handles too. Handles lerped between mismatched partners collapse or cross, and the blended cubic carries a near-cusp neither station has. Users hit this by putting two control points close together (the `80-20-hard` rail preset does exactly that): the mesh pinches, and deleting one of the pair fixes it. Measured 1.45× the endpoint curvature — **with both stations at the same knot count**, so the count-matching path never ran. Index pairing itself is the defect, not the insertion heuristic, which is why normalising every station to a common knot count (`b5293d5`) could not have fixed it. It is a malformed curve, not a malformed sampling of a good curve, which is why three rounds of fixing the ring sampler (`4cb7698`, `b5293d5`, `f5c605b`) each helped and none fixed it. **This is a convergence on BoardCAD-LE, not a departure from it:** the legacy shipped two surface models and its GUI defaulted to this one (`BezierBoardSLinearInterpolationSurfaceModel.getPointAt`, selected by `MenuBar.getCrossSectionInterpolationType`); both its STL exporter and its 3D viewer went through it. We had ported the other one. | Fairness: `loft.pinch.test.ts` measures peak discrete curvature on the tessellated mesh across 40 stations between two differently-presetted sections and requires it to stay within 5% of the sharper endpoint — the current path measures 1.45×/1.47× on the two `80-20-hard` pairs and passes at ≤1.0 after. Exactness: `tessellate.continuity.test.ts` and `stl.test.ts` loft a constant-shape prism whose stations carry 4/5/7 knots and now require agreement to 1e-5 cm and 1e-4 cm. Smoothness: `tessellate.smoothness.test.ts` bounds the sweep's second difference and its high-frequency content at 1e-5 cm. Legacy remains the oracle for everything still on the control-point model. | fix/loft-sampled-points            |

The sLinear golden **volume** band was relaxed 1e-4 → 1e-2 in the same commit
(`board.slinear.test.ts`), with a convergence-oracle test; the per-station
**area** band stays 1e-4 because the inner cross-section trapezoid
(`SLINEAR_AREA_SPLITS`) is deliberately left legacy-pinned.

The 2026-08-11 loft change leaves the app describing its surface two ways: the
3D view and the STL blend sampled points, while the reported volume, centre of
mass and the HWS ribs still blend control points. The gap is 0.04–0.39% of
volume and was scoped that way on purpose, to keep the golden fixtures out of a
mesh fix. Reconciling it means moving `getControlPointCrossSectionAreaAt` onto
`loft.ts` and re-banding `golden.json`'s volume/CoM — worth doing, deliberately
not done here.

**2026-08-22 — rail bands crossed over.** `railFacetsAt` now takes its station
section from `loftCrossSection` (`loft.ts`) rather than
`getInterpolatedCrossSection`, so the rail-band sheet sits on the same surface as
the 3D view and the STL. No table row: BoardCAD-LE has no rail bands, so no
legacy value is superseded — this is one internal model replacing another. It is
listed here because the paragraph above enumerates who is on which side, and that
list is now: **sampled points** — 3D view, STL, rail bands; **control points** —
volume, centre of mass, 2D section preview, `insertCrossSection`, DXF/PDF section
curves, HWS ribs. The magnitude on the moved caller is not small: on a longboard
carrying a different rail preset per station the blended section stood 4.96 mm
off the lofted surface, and the fitted first band read 17.0°, 65.5°, 41.5° at
three consecutive stations of a rail that is ~21° throughout. `docs/design/rail-bands.md`
("Where a station's section comes from") has the account; oracles are
`rail-facets.loft.test.ts` (re-knotting a station, which cannot change the board,
moves no printed number by more than 0.02 mm) and `loft.section.test.ts` (the
rebuilt curve tracks the exact arc-length blend to 0.076 mm and never blends a
section sharper than the stations either side of it).

## `.brd` writer (`packages/io/src/brd-writer.ts`)

`writeBrd` (the export-to-legacy path, paired with the existing `parseBrd` reader) is
**lossy relative to legacy `board.writers.BrdWriter`**: it emits the geometry fields
(p32 outline, p33 bottom, p34 deck, p35/p36 cross-sections) plus the identity/dimension
scalars our model actually carries (p1–p4 length/lengthOverCurve/thickness/centerWidth,
p7 version, p45 designer, p48 surfer, p49 comments, p51 finType, p54 model). It does **not**
write the legacy CAM/machine scalar fields (cuts, cutter diam, pivots, speeds, margins,
struts, blank/board positions — p11–p31, p43–p47, etc.) or the per-curve guide-point (`gps`)
blocks, because the kernel board model does not represent them.

This is intentional and safe to round-trip: `parseBrd` already treats every one of those
fields as optional (the geometry is rebuilt purely from p32–p36), so `parseBrd(writeBrd(b))`
reproduces the board geometry exactly (pinned in `brd-writer.test.ts`). No numeric golden
value is superseded, so there is no table row above — re-importing a written `.brd` into
BoardCAD-LE simply yields a board with default CAM settings.

## Known candidates (not yet diverged)

- **Junction constraints — unimplemented legacy locks/masks** (see
  `docs/specs/junction-constraints.md`, JC-1…JC-8). The web `enforceJunctions`
  re-snaps positions only; it does not model the legacy per-knot masks, tangent
  locks, or slaves. The gaps, pinned by the "junction-constraint spec (legacy
  parity pinning)" tests in `packages/store/src/edits.test.ts`:
  - ~~**Outline endpoint centreline pin is asymmetric**~~ — **RESOLVED** in
    `fix/junction-outline-tip-pin` (2026-06-19, table row above). The pin now
    snaps the **nose** (`knots[last]`) to `y = 0` and leaves the **tail**
    (`knots[0]`) free; the inverted nose/tail naming in `board.ts` (lines ~48/56)
    and `enforceJunctions` is corrected. Pinning only the nose `y` (vs legacy's
    both-tip mask) is the deliberate divergence recorded above.
  - ~~**Endpoint masks not modeled (JC-1/JC-2/JC-3)**~~ — **RESOLVED (x-axis)** in
    `fix/junction-outline-tip-pin` (2026-06-19, table row above): outline tail and
    deck/bottom endpoints are now x-re-snapped to their stations. Remaining gap is
    only the legacy _live un-draggable_ mask (a handle that literally cannot be
    grabbed) and the tail-y lock — deliberately left free; a render-layer concern,
    not an `enforceJunctions` re-snap.
  - ~~**Tangent-flow locks not modeled (JC-6/JC-7/JC-8)**~~ — **RESOLVED** in
    `fix/junction-outline-tip-pin` (2026-06-19, table row above): `clampMonotonicX`
    (JC-6) + `clampHandleFloor` (JC-7/JC-8) keep the curves single-valued in x and
    the tips departing outward. Remaining gap is only the legacy _live_ drag clamp
    (we re-snap post-edit instead) — interaction, not committed geometry.
  - ~~**`adjustCrossectionThickness` y-mask (JC-4 y) not modeled**~~ — **RESOLVED** in
    `fix/junction-adjust-thickness-gate` (2026-06-20, table row above): added the
    `adjustCrossSectionThickness` editor setting + store `adjustThickness` flag; when
    off, `commit`/`load` skip `adjustCrossSectionsToThicknessAndWidth`. Implemented as
    a commit-pipeline gate (not a per-knot mask), so the legacy _direct section-drag_
    y-mask itself remains unmodelled — the only residual gap.

  With JC-1…JC-8 all addressed, the junction-constraint port is **complete** save for the
  legacy _live-drag_ interaction (we re-snap/gate post-edit rather than clamping mid-drag)
  and the literally-non-grabbable handle mask, both render-layer concerns.

  These are unimplemented behaviors, not superseded golden values, so they have
  no table row (no better oracle / regenerated fixture exists yet). Promote to a
  table row only once a junction-lock layer is built and verified.

(The former adaptive-integration candidates were actioned — see the table rows
above.)
